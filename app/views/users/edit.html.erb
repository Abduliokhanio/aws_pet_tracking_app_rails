<% content_for :title, "Editing user" %>

<div class="page-header">
  <h1>✏️ Edit User</h1>
  <p class="page-subtitle">Update user information</p>
</div>

<%= render "form", user: @user %>

<div class="form-actions">
  <%= link_to "👁️ View User", @user, class: "btn-secondary" %>
  <%= link_to "📋 Back to Users", users_path, class: "btn-secondary" %>
</div>
