<% content_for :title, "New pet" %>

<div class="page-header">
  <h1>➕ New Pet</h1>
  <p class="page-subtitle">Add a new pet for <%= @user.name %></p>
</div>

<%= render "form", pet: @pet %>

<div class="form-actions">
  <%= link_to "📋 Back to Pets", user_pets_path, class: "btn-secondary" %>
</div>
